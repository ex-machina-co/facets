{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "types": {
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build", "types", "build"]
    }
  }
}
